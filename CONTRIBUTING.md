# Contributing to Laiba Badar

1. Fork → clone → `git checkout -b feature/your-idea`
2. Make focused changes (no secrets / `.env`)
3. Open a Pull Request to `main`

Download, use, fork, and improve — PRs welcome.
